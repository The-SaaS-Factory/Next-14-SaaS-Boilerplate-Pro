"use server";
//import prisma from "@/lib/db";

export const getModelByCategories = async () => {
  // const category = await prisma.userCategory.findFirst({
  //   where: {
  //     name: categoryName,
  //   },
  // });

  // if (!category) {
  //   return [];
  // }

  // const models = await prisma.user.findMany({
  //   where: {
  //     type: "MODEL",
  //     categoryId: category.id,
  //   },
  // });

  return [];
};
